import React, { useState, useEffect} from 'react';

export default function NetworkPolicies() {
    useEffect(() => {
    }, [])

    return (
        <main>
            <div cds-layout="vertical gap:lg">
                <p cds-text="title">Network Policies</p>
            </div>
        </main>
    );
}
